{
  "name": "al-wake",
  "version": "1.0.0",
  "description": "Al-Wake Smart Alarm App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed'",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
